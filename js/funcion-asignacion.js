//asignar el valor retornado

function suma(a, b) {
    return a + b;
};

var resultado = suma(5, 3);
console.log(resultado);

function crearCadena(leguajeDeProgramacion) {
    return `Mi meta es aprender ${leguajeDeProgramacion}`;
}

var meta = crearCadena("C++");
console.log(meta);

